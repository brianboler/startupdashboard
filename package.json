{
  "name": "startup-pulse",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "pipeline": "node src/run.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "verify:render": "node tools/render-check.mjs"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "rss-parser": "^3.13.0"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "vitest": "^3.0.0"
  }
}
